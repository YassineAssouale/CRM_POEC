import { UserIdConnectedDirective } from './user-id-connected.directive';

describe('UserIdConnectedDirective', () => {
  it('should create an instance', () => {
    const directive = new UserIdConnectedDirective();
    expect(directive).toBeTruthy();
  });
});
