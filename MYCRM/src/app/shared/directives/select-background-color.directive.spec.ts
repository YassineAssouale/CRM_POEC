import { SelectBackgroundColorDirective } from './select-background-color.directive';

describe('SelectBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
