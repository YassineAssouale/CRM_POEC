import { CustomerDisplayDirective } from './customer-display.directive';

describe('CustomerDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomerDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
