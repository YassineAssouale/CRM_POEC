import { OrderHtColorDirective } from './order-ht-color.directive';

describe('OrderHtColorDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderHtColorDirective();
    expect(directive).toBeTruthy();
  });
});
